"use client";

// Swap this out to eyeball whatever component you're working on, against
// whatever fake data it needs — same idea as jejaku-receipt's copy of
// this file, just with nothing app-specific to preview yet.
export default function DevPreviewContent() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-canvas-soft p-[24px]">
      <p className="text-[13px] text-ink-mute">Nothing wired up here yet — swap this in for a component preview.</p>
    </div>
  );
}
